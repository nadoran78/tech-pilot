interface:
  display_name: "Create Develop PR"
  short_description: "Validate and open a Draft PR to develop"
  default_prompt: "Use $gh-create-develop-pr to validate the current issue branch and create a Draft pull request targeting develop after explicit confirmation."
policy:
  allow_implicit_invocation: false
