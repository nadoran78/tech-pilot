interface:
  display_name: "Create GitHub Issue"
  short_description: "Draft and publish a scoped GitHub issue"
  default_prompt: "Use $gh-create-issue to prepare a scoped GitHub issue and publish it only after explicit confirmation."
policy:
  allow_implicit_invocation: false
